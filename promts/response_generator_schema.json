{
    "type": "json_schema",
    "json_schema": {
        "name": "response_generator_schema",
        "strict": true,
        "schema": {
            "type": "object",
            "properties": {
                "response_text": {
                    "type": "string",
                    "description": "The final response message to the user, incorporating insights from thinker data and original user message."
                },
                "tone_used": {
                    "type": "string",
                    "description": "The tone used in the response, reflecting the user's emotional state and context."
                }
            },
            "required": [
                "response_text",
                "tone_used"
            ]
        }
    }
}